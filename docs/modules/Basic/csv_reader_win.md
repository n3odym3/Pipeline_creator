# CSV Reader Module

Loads local tabular data sheets and processes them into separate sample files.

---

## Technical Specifications

- **Inputs**: None.
- **Outputs**: `Sample` (Type: `SAMPLE`)

---

## Usage

- Click **Load CSV** to open the local file explorer.
- Select a CSV spreadsheet.
- The reader parses columns and streams data rows as sample events on each trigger.
